# Twitter API Configuration

# API Key from twitterapi.io/?ref=0xmartian (REQUIRED - this is a paid service)
TWITTER_API_KEY=your_api_key_from_twitterapi_io

# API Base URL (optional, defaults to https://api.twitterapi.io)
TWITTER_API_BASE_URL=https://api.twitterapi.io

# Login Credentials (ALL REQUIRED for authentication)
# The wrapper will automatically get and manage cookies via the API
TWITTER_EMAIL=your_email@example.com
TWITTER_USERNAME=your_username
TWITTER_PASSWORD=your_password

# REQUIRED for login: High-quality residential proxy
# Example: http://username:password@ip:ports
# Get proxy from: https://www.webshare.io/?referral_code=soevflhx11bu
TWITTER_LOGIN_PROXY=http://username:password@ip:port

# REQUIRED: TOTP/2FA Secret
# Without this, the API will return status 400 errors
# Steps to get: Enable 2FA on Twitter, select "can't scan QR code" option
# Twitter will show a string like "ASBK646Q435UCGYI" - that's your TOTP secret
TWITTER_2FA_SECRET=your_totp_secret_key

# Rate Limiting (optional, defaults to 1 for free tier)
# Free tier: 1 request per 5 seconds
TWITTER_MAX_QPS=1

# Request Timeout in milliseconds (optional, defaults to 30000)
TWITTER_TIMEOUT=30000